/**
 * Shared Vite build defaults (target, outDir).
 * Use with mergeConfig from Vite or spread into workspace build config.
 */
export const viteBuildBase = {
  target: 'es2022',
  outDir: 'dist',
};
