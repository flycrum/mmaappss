# Sandbox agents

Root AGENTS.md for integration test sandbox. Sync will create CLAUDE.md symlink here when claude is enabled.

This file is used by the integration test harness to simulate a real repo with plugins under `.agents/plugins` (root and nested).
